#include <stdio.h>
#include<stdbool.h>
bool is_subsidezed(float unit){
  if(unit<=500)
}

int main(){
  float userunit=0;
  printf("Enter units consumed bi-monthly: ");
  scanf("%f",&userunit);
  printf("Units consumed: %.2f\n",userunit);
  
  return 0;
}